#!/usr/bin/env bash

actions=("suspend" "poweroff" "reboot")

chosen=$(printf "%s\n" "${actions[@]}" | rofi -dmenu -p "Select action:")

if [ -n "$chosen" ]; then
    if [ "$chosen" == "suspend" ]; then
        swaylock & systemctl suspend
    else
        systemctl "$chosen"
    fi
fi
